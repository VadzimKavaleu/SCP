{
    "name": "sample",
    "version": "1.0.0",
    "description": "Generated by cds-services-archetype",
    "license": "ISC",
    "repository": "<Add your repository here>",
    "scripts": {
        "build": "cds build/all --clean",
        "schema": "cds deploy --to sqlite:srv/local.db --no-save",
        "deploy": "cds deploy",
        "start-server": "mvn spring-boot:run",
        "clean": "mvn clean",
        "local-run": "npm run build-local && npm run start-server",
        "build-cloud": "mvn clean && npm run build && mvn install",
        "build-local": "mvn clean && npm run build && npm run schema && mvn install",
        "hana-run": "mvn spring-boot:run -Dspring-boot.run.profiles=defenv",
        "sqlite-run": "mvn spring-boot:run"
    },
    "dependencies": {
        "@sap/cds": "^3.21.1"
    },
    "cds": {
        "build": {
            "target": "."
        },
        "requires": {
            "db": {
                "kind": "hana",
                "model": [
                    "db",
                    "srv"
                ]
            },
            "uaa": {
                "kind": "xsuaa"
            }
        },
        "odata": {
            "version": "v4"
        }
    },
    "devDependencies": {
        "sqlite3": "^4.1.1"
    }
}
